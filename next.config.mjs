/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "weezvqaubwpmppryvitb.supabase.co",
        
      },
    ],
  }
};

export default nextConfig;
